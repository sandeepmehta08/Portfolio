name: Build Vue
on: [push]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4
    - id: Build-Vue
      uses: xRealNeon/VuePagesAction@1.0.1
      with:
        username: 'sandeepmehta08'
        reponame: 'Portfolio'
        token: ${{ secrets.GITHUB_TOKEN }} # Leave this line unchanged
